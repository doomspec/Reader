[project]
name = "reader"
version = "0.1.0"
description = "A command-line tool for reading long documents (markdown and LaTeX)"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
reader = "reader.cli:main"
reader-ls = "reader.ls:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"